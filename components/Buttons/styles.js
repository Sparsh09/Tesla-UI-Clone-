import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
    padding: 10,
  },
  text: { fontSize: 16, fontWeight: "500", textTransform: "uppercase" },
  button: {
    borderRadius: 20,
    justifyContent: "center",
    alignItems: "center",
    height: 40,
  },
});

export default styles;
